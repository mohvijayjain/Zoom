# SQLite by default; point at Postgres/MySQL by swapping the URL.
DATABASE_URL=sqlite:///./zoom.db

# Origin of the Next.js frontend, added to the CORS allow-list.
# localhost:3000 / 127.0.0.1:3000 are always allowed.
FRONTEND_URL=http://localhost:3000
