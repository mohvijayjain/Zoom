import { JoinForm } from "@/components/meeting/JoinForm";

/**
 * Inside the (dashboard) group, so it keeps the navbar chrome — matching the
 * reference, which still shows Schedule / Join / Host / Web App here.
 */
export default function JoinPage() {
  return <JoinForm />;
}
