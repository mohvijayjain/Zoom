import { ScheduleForm } from "@/components/schedule/ScheduleForm";

/** Inside (dashboard) — the reference keeps the full chrome on this page. */
export default function SchedulePage() {
  return <ScheduleForm />;
}
